using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DemoScript : MonoBehaviour
{/*
    public InventoryManager inventoryManager;
    public Items[] itemsToPickup;

    public void PickupItem(int id)
    {
        bool result = inventoryManager.AddItem(itemsToPickup[id]);

    }

    public void GetSelectedItem()
    {
        Items receivedItem = inventoryManager.GetSelectedItem(false);
        if (receivedItem != null)
        {
            Debug.Log("Received item");
        }
        else Debug.Log("no item found");
    }

    public void UseSelectedItem()
    {
        Items receivedItem = inventoryManager.GetSelectedItem(true);
        if (receivedItem != null)
        {
            Debug.Log("Use item");
        }
        else Debug.Log("was used");
    }*/
}
